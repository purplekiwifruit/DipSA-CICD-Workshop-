name: my first workflow

on:
  push
  
jobs:

  my-first-job:

    runs-on: ubuntu-latest

    steps:
      - name: checkout source code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.base_ref }}
      